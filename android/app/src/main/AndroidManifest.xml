<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recipesonline">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.Main.MainActivity"></activity>
        <activity android:name=".view.UpdateIngredient.UpdateIngredientActivity"/>
        <activity android:name=".view.SearchResults.SearchResultsActivity" />
        <activity android:name=".view.CreateRecipe.CreateRecipeActivity" />
        <activity android:name=".view.Register.RegisterActivity" />
        <activity android:name=".view.Profile.ProfileActivity" />
        <activity android:name=".view.Recipe.RecipeActivity" />
        <activity android:name=".view.Home.HomeActivity" />
        <activity android:name=".view.Login.LoginActivity" />
    </application>

</manifest>